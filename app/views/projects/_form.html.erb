<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "项目名称" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="row-fluid">
    <div class="field">
      <%= f.label :description, "项目介绍" %><br />
      <%= f.text_area :description, :class => "span9 tinymce", :rows => 40 %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit "保存" %>
  </div>
<% end %>


<%= tinymce :content_css => asset_path('tinymce.custom.css') %>
